spring:
  application:
    name: myShoppingCartProject
  datasource:
    url: jdbc:postgresql://localhost:5432/myShoppingCartProject
    username: postgres
    password: pg123
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

# Email Settings
mail:
  username: kalelofearth95@gmail.com
  password: ndbl qtkt hkil njal
  host: smtp.gmail.com
  port: 587
  protocol: smtp
  properties:
    mail:
      smtp:
        auth: true
        starttls:
          enable: true